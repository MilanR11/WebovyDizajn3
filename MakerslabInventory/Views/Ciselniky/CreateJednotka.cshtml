@model MakerslabInventory.Models.Jednotka
@{ ViewData["Title"] = "Pridať jednotku"; }

<h1>Pridať novú jednotku</h1>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="CreateJednotka">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group mb-3">
                <label asp-for="Nazov" class="control-label">Názov jednotky</label>
                <input asp-for="Nazov" class="form-control" placeholder="Napr. ks, kg, balenie..." />
                <span asp-validation-for="Nazov" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Uložiť" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-secondary">Späť</a>
            </div>
        </form>
    </div>
</div>
@section Scripts { @{await Html.RenderPartialAsync("_ValidationScriptsPartial");} }